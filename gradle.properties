# Maven Central namespace. io.github.<user> is verified automatically against
# the GitHub account (no DNS). The Java package stays net.nowhereatall.xfty -
# Central verifies the groupId namespace, not the package.
group=io.github.nilvon9wo
version=0.1.0-SNAPSHOT

org.gradle.caching=true
org.gradle.parallel=true
